import React from 'react';
import {View} from 'react-native';

interface Props {}

function __ComponentName__(props: Props) {
  return (
    <View>
    </View>
  );
}

export default React.memo(__ComponentName__);
